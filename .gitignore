# Quickshell runtime
*.qslog
*.lock

# Filter files (installed locally, not part of the source)
*.filter
.neversink-version

# User preferences and runtime state (saved locally)
.saved-*

# Local caches (tree data, scratch files) and Python bytecode
.cache/
**/__pycache__/
*.pyc
